# نصب دستی Inline Handwriting

1. از صفحهٔ Releases گیت‌هاب، فایل‌های `main.js`، `manifest.json` و `styles.css` را دانلود کنید.
2. در vault خود، پوشهٔ زیر را بسازید:

   ```text
   .obsidian/plugins/inline-handwriting-puria/
   ```

3. هر سه فایل را داخل آن پوشه قرار دهید.
4. Obsidian را Reload کنید و در **Settings → Community plugins** افزونهٔ **Inline Handwriting** را فعال کنید.

برای شروع، یک یادداشت Markdown باز کنید و دستور **Insert handwriting block** را اجرا کنید. با دکمهٔ **Edit** روی نقاشی، بوم نوشتن باز می‌شود.

این افزونه کاملاً محلی است و هیچ OCR، کلید API، ارسال اطلاعات یا telemetry ندارد.
